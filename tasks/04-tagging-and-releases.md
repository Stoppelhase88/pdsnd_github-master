# Aufgabe 4: Tags & Releases

**Ziel:** Versionieren mit semantischen Versionen & Release anlegen.

1. Tag setzen:
   ```bash
   git tag -a v0.1.0 -m "Initiale funktionale Version"
   git push origin v0.1.0
   ```
2. Optional: GitHub **Release** aus dem Tag erstellen (Changelog verlinken).
